// for organisig our code we need to organise our route into diif like then input it to main app then put it in app.use

import { Router } from "express";

const userroute = Router()


// like this 
userroute.get()


//wen done export it to the main app to use

export default userroute




//we can also use app.use in route then export the route to the main app