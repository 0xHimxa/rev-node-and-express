const {greating} = require('./1i-mod')
const path = require('path');


// console.log(greating('himxa'))
// u use path wen we want to access to specific path or join


//  return the owle directory name filename

//dirname return the directory to the curent folder only

console.log(path.basename(__filename))

//n